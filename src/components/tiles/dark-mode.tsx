import { CardHeader, CardTitle } from "../ui/card";

export function DarkMode() {
    return (
        <>
            <CardHeader>
                <CardTitle>DarkMode</CardTitle>
            </CardHeader>
        </>
    );
}
